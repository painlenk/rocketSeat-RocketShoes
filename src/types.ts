export interface Product {
  id: number;
  title: string;
  price: number;
  image: string;
  amount: number;
}

export interface ProductList {
  id: number;
  title: string;
  price: number;
  image: string;
}

export interface Stock {
  id: number;
  amount: number;
}
